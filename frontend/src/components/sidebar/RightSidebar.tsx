const RightSidebar = () => {
  return (
    <div className="flex h-full flex-col gap-4 p-4">
      <div className="text-muted-foreground text-sm">
        Right sidebar (recommended users, trending hashtags, etc.)
      </div>
    </div>
  );
};

export default RightSidebar;
